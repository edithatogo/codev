# Fixtures

Synthetic APFS fixtures will be generated here. No personal disk images or secrets are allowed.

The first committed placeholder may be non-APFS data for testing the `not_apfs` path. Real APFS fixtures require macOS generation scripts and redacted manifests.
